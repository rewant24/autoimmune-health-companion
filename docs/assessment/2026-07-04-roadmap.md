# Saha — Roadmap (2026-07-04)

Derived from [the Fable assessment](./2026-07-04-fable-assessment.md). Structured around the stated intent: **Fable 5 runs the assessment + refactor + optimization (Phases 0–2); Opus builds the end-to-end product (Phases 3–5) on the hardened foundation.** The handoff line between the two is explicit below.

Legend — Effort: S <½day · M 1–2d · L multi-day/migration. Owner: 🅕 Fable (hardening/refactor/optimize) · 🅞 Opus (feature build).

---

## Phase 0 — Emergency (🅕, today, hours not days)

Do these before anything else; none depend on each other.

1. **Rotate the Vercel protection-bypass token** and de-hardcode both scripts (`isolate-418.mjs`, `posthog-smoke.mjs`) → `process.env`. Add `gitleaks` pre-commit hook. *S — the repo is public and this is the 2nd leak.*
2. **Add ownership check to `getCheckin`** (`convex/checkIns.ts:295`) — `userId` arg + `row.userId` assert. *S — closes a live cross-user IDOR.*
3. **Vercel WAF rate-limit rule on `/api/*`** — stopgap against LLM/voice cost-abuse until auth re-keys the caps. *S–M.*
4. **Interim extract-429 fix** (`check-in/page.tsx:901-909`) — branch on `ExtractDailyCapError` for honest copy; stop silent auto-decline of metrics. *S — the full Pattern B waits for the harness, but this stops the bleeding.*

**Exit criteria:** token rotated & no literals in tracked files; `getCheckin` returns null on owner mismatch; `/api/*` rate-limited; 429 no longer silently corrupts a check-in.

---

## Phase 1 — Foundation hardening (🅕, ~1 week)

The refactor/optimize pass that makes the Opus feature-build cheap and safe. Everything here is **pre-auth debt that gets more expensive with every feature wave.** Parallelizable into the lanes shown.

### Lane 1A — Build health & CI (do first; unblocks trust in every later gate)
- Fix/remove the redacted `.env.production.local` so local `pnpm build` is green. *S.*
- Stand up the **first CI workflow**: frozen install + typecheck + `vitest run` + `next build` (needs `NEXT_PUBLIC_CONVEX_URL` repo var). Measured <7 min. Keep integration/live-LLM/e2e out of the PR gate; nightly `workflow_dispatch` for integration. *S.*
- Add **eslint flat-config** in the same PR (no linter exists today). *S.*
- Fix the **e2e webServer** to `next build && next start` (kills the false-red suite). Budget a test-auth strategy note for Phase 3. *M.*

### Lane 1B — Auth-seam prep (converts auth from a 12-file swap to a 1-file swap)
- Extract **`useUserId()`** hook — consolidate the 10+ copy-pasted `TEST_USER_KEY` constants. *S.*
- Extract **`useOnboardingGuard()`** (the natural mount point for the real auth guard). *S.*
- Remove the **18 stale `(api as any)` casts**. *S.*
- Add **`app/error.tsx` + `not-found.tsx`**. *S.*

### Lane 1C — Query optimization (must precede F06/F08)
- **Date-bound all six list/range queries** — push `gte/lte('date')` into the `by_user_date` predicate. *M.*
- Quick wins: `(userId, clientRequestId)` indexes on `doctorVisits`/`bloodWork`; date-format validation on `createCheckin`/`logIntake`; dedicated `extract.rate_limited` code on the 3 LLM routes; wire `getNextUpcomingVisit` into continuity's `upcomingEvent` stub. *S each.*
- Record the **TOCTOU-is-safe-on-Convex ADR** so it stops resurfacing. *S.*

### Lane 1D — Design-system consolidation (do before F06 clones the pattern a 4th time)
- Merge `MedicationConfirmCard` + `EventConfirmCard` → one `ConfirmCard` + `ConfirmCardStack` (cap depth, design the grouped >3 presentation once). *M.*
- Extract `Card`/`PillButton`/`Field` primitives + `lib/format/date.ts` (5 duplicated IST formatters) + shared `DeleteConfirmDialog` + `ModuleListShell`. *M.*
- **Zinc→token palette migration** of the check-in component family (15 files) — highest visual impact; ride the primitive extraction. *M.*

### Lane 1E — Doc-hygiene commit (unblocks the Opus build from inheriting contradictions)
- **Reconcile scoping.md** (the conflict-winner): strike Community-as-MVP-Pillar-4, remove the superseded mobile-verification setup flow. *M — highest-priority doc fix.*
- Refresh statuses across `system-map.md`, `build-plan.md` §3/§7, feature MD front-matter. Close the journey-bottom-cta spike (shipped as PR #25). Fix broken `scoping_ref` anchors, strike the taxonomy Billing branch, trash `~$-target-patterns.md`.
- **Close or re-spike body-map C3** — artifacts confirmed absent from all git history; the sprint's binary decision is unexecutable as written.

**Exit criteria:** CI green on PRs; `useUserId()`/guard extracted; six queries date-bounded; one `ConfirmCard`; scoping.md matches reality. **This is the "working framework" the Opus build stands on.**

---

## Phase 2 — Voice polish (🅕, parallelizable with Phase 1/3)

Sequenced per the voice research + code audit. Independent of auth.

1. **Telemetry stitch (½ day):** `voiceLog` on cap/failure/TTS-reject paths + session-outcome event; **run the never-run PostHog Layer 2 live smoke** on meetsaha.com. Quantifies the P0 before pattern work. *S.*
2. **Pattern A** (mid-loop "Heard X, doing Y" acknowledgment) — ~1 day, zero blockers, reuses the decline-ack fire-and-forget TTS precedent. Addresses the "feels mechanical" theme all three research reports converged on. *S.*
3. **`VoiceProvider.abort()`** — release mic without POSTing; wire to `BAIL_TO_TAPS`/unmount. Privacy fix. *S–M.*
4. **A2 e2e voice harness** (A2.2–A2.7) — includes the repo's first Actions workflow; **add the scenario: extract stubbed 429 mid-loop → fallback → assert `voice_rearm_fired`.** This makes it the gate for Pattern B. *M.*
5. **Pattern B + D bundle** (after harness green) — B forks by error class (cap→taps-only, transient→retry); salvage `lib/checkin/extract-failure-fallback.ts` from the retained PR #21 branch. *M.*
6. **Pattern C** deferred post-pricing (pending Sarvam pace-control feasibility). *M.*

**Write the T5 canonical voice spike** once you pick the Pattern A phrasing approach (locked-catalog vs LLM-dynamic — one open question from the research).

---

## ══ HANDOFF: Fable → Opus ══

**Opus takes over here.** Phases 0–2 leave a hardened, CI-gated, single-seam foundation with a reconciled scoping doc. Everything below is net-new feature construction against that framework, following the project-playbook (scoping → parallel build subagents → parallel review → fix → ship).

---

## Phase 3 — Auth (🅞, the load-bearing dependency — blocks revenue)

**Decision C + DLT clock is the wall-clock long pole — start it day one, in parallel with everything else.**

1. **Day 1:** lock auth Decision C (MSG91 primary) → kick off MSG91 **DLT registration (3–7 business days)**. Walk Decisions D/F/E/G/H in parallel while the clock runs. *F (profile location) must precede E (migration scope).*
2. **Build (Phase 3 proper — owns the Convex dev singleton; no other convex-touching lane runs in parallel):**
   - Replace client `userId` args with `ctx.auth.getUserIdentity()` across all 32 handlers (mechanical thanks to existing ownership checks).
   - New **`users`/`profiles` table** + localStorage→Convex profile migration (snapshot prod first: `npx convex export`).
   - Re-key the extract **cost guard on server-derived identity**; add per-identity counters to the med/event routes.
   - Re-gate `devSeed.wipeUser`; lock `extractAttempts.getCount` behind auth.
   - `clientRequestId` unique indexes (ADR-034) ship inside this build.
   - Update PostHog `identify` off the localStorage stub → auth `distinct_id`; update the harness D4 assertion in the same PR.
   - Update all e2e/integration seed helpers (they seed `saha.testUser.v1` — auth invalidates them).

**Parallel-safe during Phase 3 build:** voice Pattern A, F03/F06 chunking cycles (markdown only), A2 harness.

---

## Phase 4 — Revenue path (🅞)

The critical path to first paying user. **The paid tier's entire value prop is unbuilt — this is the real revenue work, not the billing wiring.**

1. **Onboard the first 50** from the immutable waitlist (free) once auth ships.
2. **Build F03 Patterns C1 + F06 Doctor Report** — the paid-tier value prop. *Decide the F06 fork first: F03 C1 before F06 (for the PDF patterns summary), or F06 without the patterns section.* F06 needs the intake dose/name snapshot fix (Phase 1C/P2) for report accuracy.
3. **Core-funnel PostHog events** — the pricing decision's required data source (instrumentation is voice-only today). Seam already exists.
4. **Pricing revisit** post-50 per backlog #17 (₹199/mo · ₹1,499/yr IN · $4.99/mo · $39/yr intl). Lock the timezone convention before the international tier.
5. **Razorpay + Stripe** wiring + paywall gates (Memory 30-day limit, report 1/mo quota, Patterns paid-only).

**Seed fixtures required:** prod has ~5 dormant waitlist signups + smoke rows only; F03's 14-day and F06's 30-day windows need seed data for development (the Sonakshi journey test already specifies the approach).

---

## Phase 5 — Remaining pillars (🅞, post-revenue)

- **F07 Prepare-for-Visit** (depends on F05 ✓ + F06).
- **F08 Journey aggregation** — pre-reqs: flatten `bloodWork.markers[]` → relational `bloodWorkMarkers` with **canonical marker names** (prod migration, do before data grows); the date-bounded queries from Phase 1C; decide `/visits`+`/blood-work` nav home. Depends on F02/F03/F05/F06.
- **F09 Community** — fully parallel-safe (no data deps), de-facto post-launch.
- **Backlog promotions:** Memory tap-to-detail (22.6 — "evidence" is the job-to-be-done; a flat list isn't evidence-grade); named-positive medication extraction + clarification turn (22.7 — voice-first capture covenant currently broken); cross-table search (#23 — sequence right after auth).
- **Leave in backlog:** HIPAA/BAA (H1–H4, threshold-gated), flare↔dose chart (#19), wearables, native apps.

---

## The dependency spine (what blocks what)

```
Phase 0 emergency ─┐
                   ├─► Phase 1 hardening ─► [HANDOFF] ─► Phase 3 AUTH ─┬─► first-50 onboard ─► Phase 4 revenue
Phase 2 voice ─────┘        (CI, seams,                  (Decision C    │      (F03+F06+funnel+pricing+billing)
   (independent)            queries, tokens,              DLT clock =    │
                            scoping.md)                   long pole)     └─► Phase 5 (F07, F08*, F09)
                                                                              *F08 needs marker-flatten migration
```

**Hard blockers:** Auth blocks first-50, per-user rate limits, cross-table search, pricing enforcement, and the irreversible stub-userId migration. F06 blocks F07. F08 needs the marker-flatten. Pattern B is hard-blocked on the A2 harness. The MSG91 DLT clock is the single longest wall-clock item — **starting it is P0 for scheduling even though the code is trivial.**

## Immediate next actions (this session or next)
1. 🔴 Rotate the bypass token (Phase 0.1) — genuinely today.
2. Ship Phase 0.2–0.4 (getCheckin owner check, WAF rule, interim 429 fix).
3. Decide the **auth Decision C lock** so the DLT clock can start (7 weeks parked).
4. Pick the Fable hardening lane to run first — recommend **1A (CI) then 1B/1C in parallel**.
